<Window x:Class="ActivityAgent.Service.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SasWatch Activity Agent" 
        Height="550" Width="650"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e"
        Foreground="White"
        ResizeMode="CanResizeWithGrip"
        MinWidth="500" MinHeight="400">

    <Window.Resources>
        <!-- Modern dark theme styles -->
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#e94560"/>
        </Style>
        
        <Style x:Key="SubHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#aaa"/>
        </Style>
        
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#e94560"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#888"/>
        </Style>
        
        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ff6b6b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#0f3460"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a4f8a"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="SasWatch Activity Agent" Style="{StaticResource HeaderText}"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Margin="0,0,10,0" Fill="Gray"/>
                <TextBlock x:Name="StatusText" Text="Initializing..." Style="{StaticResource SubHeaderText}" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <!-- Main Content with Tabs -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#0f3460"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#e94560"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>
            
            <!-- Status Tab -->
            <TabItem Header="Status">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                
                <!-- Connection Status Card -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Connection Status" Style="{StaticResource CardTitle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="API URL:" Style="{StaticResource StatLabel}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ApiUrlText" Text="Not configured" Style="{StaticResource StatValue}" TextTrimming="CharacterEllipsis"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Connected:" Style="{StaticResource StatLabel}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ConnectedText" Text="No" Style="{StaticResource StatValue}" Margin="0,5,0,0"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Last Sync:" Style="{StaticResource StatLabel}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="LastSyncText" Text="Never" Style="{StaticResource StatValue}" Margin="0,5,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Statistics Card -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Statistics" Style="{StaticResource CardTitle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock x:Name="EventsSentText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#4ecca3" HorizontalAlignment="Center"/>
                                <TextBlock Text="Events Sent" Style="{StaticResource StatLabel}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock x:Name="EventsQueuedText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#ffc107" HorizontalAlignment="Center"/>
                                <TextBlock Text="Queued" Style="{StaticResource StatLabel}" HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock x:Name="ErrorCountText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#e94560" HorizontalAlignment="Center"/>
                                <TextBlock Text="Errors" Style="{StaticResource StatLabel}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Monitoring Status Card -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Active Monitors" Style="{StaticResource CardTitle}"/>
                        <WrapPanel>
                            <Border x:Name="AppMonitorBadge" Background="#0f3460" CornerRadius="12" Padding="10,5" Margin="0,0,8,8">
                                <TextBlock Text="📱 Applications" FontSize="11"/>
                            </Border>
                            <Border x:Name="WindowMonitorBadge" Background="#0f3460" CornerRadius="12" Padding="10,5" Margin="0,0,8,8">
                                <TextBlock Text="🪟 Window Focus" FontSize="11"/>
                            </Border>
                            <Border x:Name="BrowserMonitorBadge" Background="#0f3460" CornerRadius="12" Padding="10,5" Margin="0,0,8,8">
                                <TextBlock Text="🌐 Browser URLs" FontSize="11"/>
                            </Border>
                            <Border x:Name="NetworkMonitorBadge" Background="#333" CornerRadius="12" Padding="10,5" Margin="0,0,8,8" Visibility="Collapsed">
                                <TextBlock Text="📡 Network" FontSize="11" Foreground="#666"/>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Recent Activity Card -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Recent Activity" Style="{StaticResource CardTitle}"/>
                        <ListBox x:Name="RecentActivityList" 
                                 Background="Transparent" 
                                 BorderThickness="0"
                                 MaxHeight="120"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" 
                                               FontFamily="Consolas" 
                                               FontSize="10" 
                                               Foreground="#aaa"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- Logs Tab -->
            <TabItem Header="Logs">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Refresh" Click="RefreshLogs_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Padding="15,5"/>
                        <Button Content="Clear" Click="ClearLogs_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Padding="15,5"/>
                        <CheckBox x:Name="AutoScrollCheckBox" Content="Auto-scroll" IsChecked="True" Foreground="#aaa" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="LogsTextBox" 
                                 Background="#0a0a1a"
                                 Foreground="#4ecca3"
                                 FontFamily="Consolas"
                                 FontSize="11"
                                 IsReadOnly="True"
                                 TextWrapping="NoWrap"
                                 BorderThickness="0"
                                 Padding="10"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Settings" Click="Settings_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
            <Button Content="Open Dashboard" Click="OpenDashboard_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
            <Button Content="Hide to Tray" Click="HideToTray_Click" Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Grid>
</Window>

